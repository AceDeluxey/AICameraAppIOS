# Install in an isolated Python environment. Record exact resolved versions in the
# generated conversion manifest before accepting any model artifact.
coremltools==8.3.0
tensorflow==2.12.0; platform_system != "Darwin" or platform_machine != "arm64"
tensorflow-macos==2.12.0; platform_system == "Darwin" and platform_machine == "arm64"
